/**
 * Settings / config UI lands in Phase 3.
 * Keep this stub so routing/docs can reference the eventual surface.
 */
export function SettingsPage() {
  return (
    <section className="panel">
      <p className="eyebrow">Phase 3</p>
      <h1>Device settings</h1>
      <p className="lede">
        Brightness, theme tokens, Wi‑Fi/MQTT endpoints, and layout presets will be configurable
        here without flashing firmware.
      </p>
    </section>
  )
}
