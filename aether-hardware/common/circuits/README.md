# Reusable circuit blocks

Share these across SKUs (`dash-s35`, `mini28`, `m40`):

| Block | Purpose |
|---|---|
| `usb-c-esd/` | USB-C CC + ESD protection snippet |
| `ldo-3v3/` | 5V to 3.3V regulation notes |
| `ec11-encoder/` | Encoder + switch reference |

Copy/adapt into each product schematic rather than maintaining divergent one-offs.
